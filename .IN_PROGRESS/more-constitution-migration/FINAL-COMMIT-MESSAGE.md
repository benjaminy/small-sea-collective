Reframe the Team Constitution as a retained signed evidence DAG rather than one lineage that must converge on a globally authoritative projection.
Document versioned reordering-resilient projections with durable basis and state digests, typed visibility and acceptance evidence, duration-based identity and authority evidence, local repudiation and ratification, living team continuations, branch-local failure, clone-relative Core adoption, and forward-only application-declared repair.
Treat visibility acknowledgments as accountable claims rather than possession proofs and surface dormancy without a protocol expiration threshold.
Adopt minimum-dependency causal contexts with fuller frontier disclosure as deliberate visibility evidence, and let post-hoc analyses surface selective, stale, or incompatible patterns without automatically proving deception.
Make adoption into a clone's live Core database a local non-pruning boundary without claiming physical immortality, universal availability, or a globally complete evidence set.
Bar `team_id` from carrying authorization weight, place suspension and resource policy under each receiving clone's control with visible local override, and record a candidate app-to-Core basis anchor as an inspectable signed claim rather than a possession or freshness proof.
Add witnessed receipt — presence-bound, never automated — as a candidate acknowledgment meaning.
Route effect-gating on witnessed receipts and locally observed silence into configurable team policy while record meanings stay fixed and hard protocol floors remain reserved for unrecoverable actions.
Treat numeric projection revisions as ephemeral cache/display state rather than durable decision-basis fields.
State non-pruning as an obligation on a well-behaved implementation rather than a peer-verifiable property, since never adopting and dropping after adoption publish the same absence; absence means unavailable, never non-existent.
Record that adoption inside a Git-carried SQLite database is an active merge operation whose declined and parked state must persist outside the live Core database.
Require any policy resolution of concurrent incompatible evidence to be justified by evidence content, closing the loophole that let identifier, timestamp, table, and arrival order back in under a policy name.
Mark witnessed receipt as the weakest rung and the only evidence no recipient can check.
Add adoption to the question taxonomy and drop the `permanent` vocabulary from retention-sense prose instead of defending it with a definition.

Pause and re-plan the #165 envelope migration before implementation, while recording that the envelope-only slice itself grants no authority and is not blocked by the production-authority prerequisites.
Retain the shared full-digest, signed-version, and team-binding decisions while withdrawing the single-tip frontier, global admission finalization, blanket proposal invalidation, whole-view failure, and one-resolver assumptions.
Treat `team_id` as a technical replay domain rather than stable social identity, bound authentic-device resource exposure, and acknowledge that indirect personal metadata remains even when direct identity payload is separable.
