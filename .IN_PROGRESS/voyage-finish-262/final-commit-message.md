Finish the bootstrap trust transcript with explicit identity continuity and per-team evidence delivery from an empty store.
Keep signed delegation, local recognition, historical-work acceptance and future key release as separate claims, and preserve the weaker evidence when a later step relies on it.

Add executable comparisons and a case-to-evidence review packet.
Correct the retention boundary using the actual Git verifier: missing old file content differs from missing commit ancestry.
Runtime provisioning, scoped authority wiring and the short-string wire ceremony remain explicitly unimplemented.

The bounded #266 follow-up uses actual signed Git commits to show why neither a historical signer union nor a current-only signer set expresses finite accepted history and new-work authority.
It preserves missing basis and creation-time evidence as separate limits, without changing runtime verification.
