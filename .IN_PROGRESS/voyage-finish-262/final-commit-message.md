Finish the bootstrap trust transcript with explicit identity continuity and authenticated per-team evidence delivery from an empty store.
Keep signed delegation, local recognition, finite historical acceptance and future key release separate, preserving missing proof when later steps rely on weaker evidence.
Add executable comparisons and a review packet; correct the retention boundary using the actual Git verifier.

The bounded #266 follow-up distinguishes cryptographic signature evidence from scoped authority, demonstrates both naive signer-set failures, and exercises captured-view decisions and retained pins through actual local fetches.
Document the simulated inputs and evidence limits; runtime provisioning, authority wiring and the short-string wire ceremony remain unimplemented.
