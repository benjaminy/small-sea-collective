[project]
name = "cod-sync"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.35.0",
    "pyyaml>=6.0.3",
    "requests>=2.32.5",
]

[project.scripts]
cod-sync = "cod_sync.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
