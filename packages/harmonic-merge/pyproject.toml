[project]
name = "harmonic-merge"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
harmonic-sqlite-merge = "harmonic_merge.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
