[project]
name = "small-sea-manager"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "small-sea-client",
    "cod-sync",
    "harmonic-merge",
    "click>=8.0",
    "cryptography>=46.0.3",
    "fastapi[standard]>=0.118.0",
    "jinja2>=3.1",
    "plyer>=2.1.0",
    "sqlalchemy>=2.0.44",
]

[project.scripts]
small-sea-manage = "small_sea_manager.cli:cli"

[tool.uv.sources]
small-sea-client = { workspace = true }
cod-sync = { workspace = true }
harmonic-merge = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
