[project]
name = "small-sea-note-to-self"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "cuttlefish",
]

[tool.uv.sources]
cuttlefish = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
