[project]
name = "splice-merge"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
splice-sqlite-merge = "splice_merge.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
