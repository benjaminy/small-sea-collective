[tool.uv.workspace]
members = ["packages/*", "devtools/*"]

[tool.uv]
dev-dependencies = [
    "boto3>=1.40.53",
    "pytest>=8.4.2",
    "small-sea-hub",
    "small-sea-client",
    "small-sea-manager",
    "small-sea-note-to-self",
    "shared-file-vault",
    "cod-sync",
    "splice-merge",
    "respx>=0.22.0",
]

[tool.uv.sources]
small-sea-hub = { workspace = true }
small-sea-client = { workspace = true }
small-sea-manager = { workspace = true }
small-sea-note-to-self = { workspace = true }
shared-file-vault = { workspace = true }
cod-sync = { workspace = true }
cuttlefish = { workspace = true }
splice-merge = { workspace = true }
