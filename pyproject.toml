[tool.uv.workspace]
members = ["packages/*"]

[tool.uv]
dev-dependencies = [
    "boto3>=1.40.53",
    "pytest>=8.4.2",
    "small-sea-hub",
    "small-sea-client",
    "corncob",
]

[tool.uv.sources]
small-sea-hub = { workspace = true }
small-sea-client = { workspace = true }
corncob = { workspace = true }
