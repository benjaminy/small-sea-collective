[tool.uv.workspace]
members = ["packages/*"]

[tool.uv]
dev-dependencies = [
    "boto3>=1.40.53",
    "pytest>=8.4.2",
    "small-sea-hub",
    "small-sea-client",
    "small-sea-manager",
    "shared-file-vault",
    "cod-sync",
    "harmonic-merge",
    "respx>=0.22.0",
]

[tool.uv.sources]
small-sea-hub = { workspace = true }
small-sea-client = { workspace = true }
small-sea-manager = { workspace = true }
shared-file-vault = { workspace = true }
cod-sync = { workspace = true }
harmonic-merge = { workspace = true }
